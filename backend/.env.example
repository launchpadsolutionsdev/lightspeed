# Database
DATABASE_URL=postgresql://user:password@host:5432/lightspeed

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=7d

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Microsoft OAuth (Azure AD / Microsoft Entra)
MICROSOFT_CLIENT_ID=your-microsoft-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret

# Anthropic Claude API
ANTHROPIC_API_KEY=sk-ant-api-key-here
ANTHROPIC_MODEL=claude-sonnet-4-6

# Frontend URL (for CORS and invite links)
FRONTEND_URL=https://www.lightspeedutility.ca

# Trial period (days)
TRIAL_DAYS=14

# Email — Gmail SMTP (requires App Password, not your regular password)
# To generate an App Password: Google Account → Security → 2-Step Verification → App passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password
FROM_EMAIL=your-email@gmail.com
FROM_NAME=Lightspeed

# Stripe (optional - for billing)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Server
PORT=3001
NODE_ENV=development
